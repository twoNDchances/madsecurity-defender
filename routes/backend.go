package routes

import (
	// "madsecurity-defender/controllers"

	"github.com/gin-gonic/gin"
)

func RouteBackend(router *gin.Engine) {
	// router.Use().Any("/*backendPath", controllers.ReturnBackend)
}